# nlp-project

Requirements:
transformers
whisper
gradio

Import the required module for text to speech conversion
`pip install pyttsx3`
`pip install google-search-results`
`pip install openai`

Overview:

Goal of the project:

Future goals:
